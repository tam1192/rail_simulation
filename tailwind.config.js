// tailwind.config.js
module.exports = {
    // 'selector' (または v3 以前なら 'class') を指定することで、
    // <html class="dark"> の有無でダークモードを切り替えられるようになります
    darkMode: "selector",
    theme: {
        extend: {},
    },
    plugins: [],
};
